"use strict"

import dependencies from 'dependencies';
import {Dependencies} from 'dependencies';

export default class ModuleName extends Dependencies {
  static classMethod() {

  }

  _defaults() {
    return {};
  }

  _configure(opts) {

  }

  constructor(opts) {
    
  }
}
