import assert from 'power-assert';
import deps from 'deps';

import ModuleName from '../';

describe('ModuleName', () => {
  describe('.test()', () => {
    it('case', () => {
      assert(true);
    });
  });
});
