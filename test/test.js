import assert from 'power-assert';
import deps from 'deps';

import ES6ModuleName from '../src/es6-module-name';

// .test()
describe('.test()', () => {
  it('test', () => {
    assert(true);
  });
});
